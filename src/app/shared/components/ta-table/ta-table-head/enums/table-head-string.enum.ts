export enum TableHeadStringEnum {
    REORDER = 'reorder',
    RESIZE = 'resize',
    SORT = 'sort',
    REMOVE_COLUMN = 'remove-column',
    PIN_COLUMN = 'pin-column',
    SELECTION = 'selection',
    SELECTED_OPTIONS = 'selected-options',
}
