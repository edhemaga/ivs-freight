export enum Commands {
    CONFIRM = 'confirm',
    CANCEL = 'cancel',
    ENTER = 'Enter',
    ESCAPE = 'Escape',
}

export enum StopTypes {
    EMPTY = 'EMPTY',
    LOADED = 'LOADED',
}

export enum AddressType {
    LONG_ADDRESS = 'longAddress',
    SHORT_ADDRESS = 'shortAddress',
}

export enum AddressLayers {
    ADDRESS = 'Address',
    LOCALITY = 'Locality',
}
