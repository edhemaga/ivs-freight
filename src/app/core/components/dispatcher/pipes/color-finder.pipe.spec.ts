import { ColorFinderPipe } from './color-finder.pipe';

describe('ColorFinderPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
