import { ModalModelData } from '../../models/modal-input.model';

export class LoadModalConstants {
    static defaultCardsValues: ModalModelData = {
        numberOfRows: 4,
        checked: true,
        front_side: [],
        back_side: [],
    };
}
