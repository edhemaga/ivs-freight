import { GetNumbersPipe } from './get-numbers.pipe';

describe('GetNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
