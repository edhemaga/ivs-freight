import { TrailerCardTypes } from '@pages/trailer/pages/trailer-card-modal/models/trailer-card-types.model';

export interface TrailerCardData {
    active: TrailerCardTypes;
    inactive: TrailerCardTypes;
}
