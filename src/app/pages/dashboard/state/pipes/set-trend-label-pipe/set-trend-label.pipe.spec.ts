import { SetTrendLabelPipe } from './set-trend-label.pipe';

describe('SetTrendLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SetTrendLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
