import { MilesIconPipe } from '@pages/miles/pipes/miles-icon.pipe';

describe('MilesIconPipe', () => {
    it('create an instance', () => {
        const pipe = new MilesIconPipe();
        expect(pipe).toBeTruthy();
    });
});
